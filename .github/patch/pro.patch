diff --git a/src/plus/gk/utils/checkin.utils.ts b/src/plus/gk/utils/checkin.utils.ts
index 27b07dd..0a01694 100644
--- a/src/plus/gk/utils/checkin.utils.ts
+++ b/src/plus/gk/utils/checkin.utils.ts
@@ -152,10 +152,25 @@ export function getSubscriptionFromCheckIn(
 		activeOrganization = organizationsWithNoLicense[0];
 	}

+	const plan = getSubscriptionPlan(
+		'pro',
+		false,
+		0,
+		undefined,
+		data.user.firstGitLensCheckIn != null
+			? new Date(data.user.firstGitLensCheckIn)
+			: data.user.createdDate != null
+				? new Date(data.user.createdDate)
+				: undefined,
+		undefined,
+		undefined,
+		data.nextOptInDate,
+	);
+
 	return {
 		plan: {
-			actual: actual,
-			effective: effective,
+			actual: plan,
+			effective: plan,
 		},
 		account: account,
 		activeOrganization: activeOrganization,
diff --git a/src/plus/gk/utils/subscription.utils.ts b/src/plus/gk/utils/subscription.utils.ts
index a0f87fb..f4cc143 100644
--- a/src/plus/gk/utils/subscription.utils.ts
+++ b/src/plus/gk/utils/subscription.utils.ts
@@ -28,7 +28,12 @@ export function compareSubscriptionPlans(
 	return getSubscriptionPlanOrder(planA) - getSubscriptionPlanOrder(planB);
 }

+const isPaid: boolean = true;
+
 export function computeSubscriptionState(subscription: Optional<Subscription, 'state'>): SubscriptionState {
+	if (isPaid) {
+		return SubscriptionState.Paid;
+	}
 	const {
 		account,
 		plan: { actual, effective },
